# NOVA Assets

Static assets, launcher icons, and branding resources.

## Structure

```
assets/
├── icons/            # Adaptive launcher icons (foreground, background, monochrome)
├── branding/         # Logo variants (SVG, PNG, dark/light)
└── splash/           # Splash screen assets
```
